*shell_commons* - utils common to shell/testing
===============================================

Installation:
-------------

Adjust dirs in `src/.erlang`.

``` shell
ln -s ~/Work/shell_commons/src/.erlang ~/.erlang
```

